# Agent Instructions

This repository uses a lightweight OpenSpec workflow.

- Stable requirements live in `openspec/specs/`.
- Planned or in-flight work lives in `openspec/changes/`.
- Prefer small, well-scoped changes that keep curriculum content organized by top-level directory.
- For larger changes, update the relevant OpenSpec artifacts before broad repository edits.
