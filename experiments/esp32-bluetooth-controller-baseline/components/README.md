# Generated components

Run `scripts/bootstrap-dependencies.sh` from this project directory before the
first build. The script downloads checksum-pinned upstream archives and places
their required ESP-IDF components here. Generated component sources are ignored
so this repository does not vendor roughly 80 MB of third-party source.

See `DEPENDENCIES.md` for exact revisions, sources, and license obligations.
