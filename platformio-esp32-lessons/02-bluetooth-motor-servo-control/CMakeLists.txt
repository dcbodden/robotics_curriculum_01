# The following boilerplate must remain in this order for ESP-IDF CMake.
cmake_minimum_required(VERSION 3.16)

include($ENV{IDF_PATH}/tools/cmake/project.cmake)

project(esp32_bluetooth_motor_servo_control)
