# OpenSpec

This repository keeps lightweight OpenSpec artifacts here.

- `specs/` captures durable repository and curriculum requirements
- `changes/` captures scoped proposals and task lists for meaningful changes
